# RFIndex DecapCMS POC

This repository is a proof-of-concept for using Decap CMS to manage the `radios` data for rfindex.com.

## Setup

1. Install the Git Gateway proxy server:

   ```bash
   npm install -g netlify-cms-proxy-server